# PyPI twikit is patched at runtime via scripts/lib/twikit_patch.py (KEY_BYTE fix).
# Optional maintained fork with additional fixes:
# twikit @ git+https://github.com/unclecode/twikit.git
twikit>=2.3.3
httpx>=0.27.0
